{
  "framework": "nextjs",
  "buildCommand": "cd real-estate-platform && npm install --legacy-peer-deps && npx prisma generate && npm run build",
  "outputDirectory": "real-estate-platform/.next",
  "installCommand": "cd real-estate-platform && npm install --legacy-peer-deps"
}
